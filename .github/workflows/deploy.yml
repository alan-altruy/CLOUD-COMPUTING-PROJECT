name: Deploy via SSH Kubernetes

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy on server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
              cd ~/PROJET/Image_Search_Engine_2026
              git pull origin main
              chmod +x mk/deploy.sh
              ./mk/deploy.sh
